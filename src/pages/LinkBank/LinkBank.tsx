import { View, Text } from 'react-native';
import React from 'react';
import CustomHeader from '../../components/CustomHeader';

const LinkBank = () => {
  return (
    <View>
      <CustomHeader title={'Link your bank!'} />
      <Text>{'LinkBank'}</Text>
    </View>
  );
};

export default LinkBank;
